package org.example;

import org.junit.jupiter.api.Test;

import static org.junit.jupiter.api.Assertions.*;

class GeneratorDistributionTest {

    @Test
    void generatePoisson() {
        GeneratorDistribution generatorDistribution = new GeneratorDistribution();
        //System.out.println(generatorDistribution.generatePoisson20());
    }
}